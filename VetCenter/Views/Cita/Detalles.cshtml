@model VetCenter.Models.Cita
@{
    ViewData["Title"] = "Detalle de Cita";
    Layout = "_PanelLayout";
}

<div class="card" style="border: 1px solid #dee2e6;">
    <div class="card-header bg-white d-flex justify-content-between align-items-center py-3">
        <h5 class="mb-0 text-dark">
            <i class="bi bi-person me-2"></i> Detalle de la Cita
        </h5>
        <div>
            <a asp-action="Editar" asp-route-id="@Model.Id" class="btn btn-outline-primary btn-sm">
                <i class="fa-solid fa-pen-to-square me-1"></i> Editar
            </a>
            <a asp-action="Index" class="btn btn-outline-secondary btn-sm ms-2">
                <i class="fa-solid fa-arrow-left me-1"></i> Volver
            </a>
        </div>
    </div>
    <div class="card-body">
        <div class="row mb-3">
            <div class="col-md-2 fw-bold">ID:</div>
            <div class="col-md-10">@Model.Id</div>
        </div>
        <div class="row mb-3">
            <div class="col-md-2 fw-bold">Fecha:</div>
            <div class="col-md-10">@Model.Fecha.ToString("yyyy-MM-dd")</div>
        </div>
        <div class="row mb-3">
            <div class="col-md-2 fw-bold">Hora:</div>
            <div class="col-md-10">@Model.Hora.ToString(@"hh\:mm")</div>
        </div>
        <div class="row mb-3">
            <div class="col-md-2 fw-bold">Motivo:</div>
            <div class="col-md-10">@Model.Motivo</div>
        </div>
        <div class="row mb-3">
            <div class="col-md-2 fw-bold">Estado:</div>
            <div class="col-md-10">@(Model.Estado ? "true" : "false")</div>
        </div>
    </div>
</div>